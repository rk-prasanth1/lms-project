pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                sh 'cd webapp && sudo npm install'
                sh 'cd webapp && npm run build'
            }
        }
       
        stage('Deploy') {
            steps {
                echo 'Deploying....'
            }
        }
    }
}
