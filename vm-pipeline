pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                sh 'cd webapp && npm install'
               
            }
        }
       
        stage('Deploy') {
            steps {
                echo 'Deploying....'
            }
        }
    }
}
